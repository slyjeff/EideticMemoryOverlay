using EideticMemoryOverlay.PluginApi.LocalCards;

namespace EideticMemoryOverlay.PluginApi.Interfaces {
    public interface ILocalCardEditor {
        void SetCard(EditableLocalCard selectedCard);
    }
}
